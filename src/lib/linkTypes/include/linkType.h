#ifndef __LINK_TYPE_H_INCLUDED__
#define __LINK_TYPE_H_INCLUDED__

#include <map>
#include <string>

class LinkType {
    virtual ~LinkType() = 0;
public:
    int getIfacesSupported();
};

static std::map

#endif